export function trackIntentClick(_eventName, _source) {
  // No-op analytics fallback so click handlers are safe without blocking render.
}

export function trackEvent(_eventName, _payload) {
  // No-op analytics fallback so event tracking is safe without blocking render.
}
